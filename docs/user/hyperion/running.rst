Running Hyperion
----------------

When installed, Hyperion should be running automatically. If it is not running, it can be (re)started from GDA by 
invoking ``hyperion_restart(udc_mode=True)`` from the Jython console.
